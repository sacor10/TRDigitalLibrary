vaderSentiment>=3.3.2
# libsql client speaks the same wire protocol as @libsql/client (TypeScript).
# Used by python/sentiment.py to read/write the library DB on Turso (or a
# local file: URL fallback for dev).
libsql-client>=0.3.1
